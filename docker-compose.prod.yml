name: radio-analytics-prod

services:
  radio-analytics:
    build: .
    image: ${CONTAINER_REGISTRY_REPO}/radio-analytics:latest
    container_name: radio-analytics
    restart: unless-stopped
    networks:
      - radio-net
    env_file:
      - .env.prod
    environment:
      - SPRING_PROFILES_ACTIVE=prod